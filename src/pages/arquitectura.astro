<div class="map-navigation-bar">
  <a href="/" class="nav-logo-link">
    <span class="font-fugaz logo-brand">CORE NEXUS FIVE</span>
  </a>
  <button class="nav-menu-btn" onclick="console.log('Abrir menú')">
    MENÚ ☰
  </button>
</div>

<section class="architecture-wrapper">

  <div class="info-intro-card">
    <div class="card-shadow"></div>
    <div class="card-content">
      <h4 class="font-fugaz">EL OLIMPO DE LOS DATOS</h4>
      <p>Este mapa conceptual interactivo desarma nuestra arquitectura desde su base para comprender su lógica.</p>
      <div class="analogy-box">
        <span class="handwritten-title">¿Por qué Grecia?</span>
        <p>Pasa el cursor sobre los círculos para iluminar el halo de luz y revelar el popup explicativo.</p>
      </div>
    </div>
  </div>

  <svg class="architecture-lines" viewBox="0 0 1600 1500" preserveAspectRatio="none">
    <defs>
      <linearGradient id="main-vertical-flow" x1="0%" y1="0%" x2="0%" y2="100%">
        <stop offset="0%" stop-color="#ffffff" stop-opacity="0.1" />
        <stop offset="50%" stop-color="#ffffff" stop-opacity="0.8" />
        <stop offset="100%" stop-color="#ffffff" stop-opacity="0.1" />
      </linearGradient>

      <linearGradient id="side-diagonal-flow" x1="0%" y1="0%" x2="100%" y2="100%">
        <stop offset="0%" stop-color="#ffffff" stop-opacity="0.1" />
        <stop offset="50%" stop-color="#ffffff" stop-opacity="0.7" />
        <stop offset="100%" stop-color="#ffffff" stop-opacity="0.1" />
      </linearGradient>
    </defs>

    <path d="M800 50 L800 1350" class="main-map-line animated-main-flow" />

    <path d="M800 135 L1080 255" class="side-map-line animated-side-flow" /> 
    <path d="M800 600 L530 720" class="side-map-line animated-side-flow" />  
    <path d="M800 600 L1070 720" class="side-map-line animated-side-flow" /> 
  </svg>

  <div class="map-row row-s3">
    <div class="node node-large">
      <div class="node-content"><img src="/s3-logo.png" alt="S3" class="node-logo" /></div>
      <div class="node-popup"><p><strong>Gaia:</strong> S3 actúa como la raíz primordial del almacenamiento.</p></div>
    </div>
    <div class="map-side-note note-right">
      <svg class="marker-arrow" viewBox="0 0 120 80">
        <path d="M20,40 Q50,35 110,40" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round"/>
        <path d="M80,15 L112,41 L80,65" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <div class="note-text-block">
        <span class="service-title-handwritten">S3</span>
        <span class="map-handwritten-text">Almacenamiento raíz<br>del sistema</span>
      </div>
    </div>
  </div>

  <div class="map-row row-iam">
    <div class="node node-small">
      <div class="node-content"><img src="/iam-logo.png" alt="IAM" class="node-logo" /></div>
      <div class="node-popup popup-left"><p><strong>Cerbero:</strong> Gestión de accesos de seguridad periférica.</p></div>
    </div>
    <div class="map-side-note note-right">
      <svg class="marker-arrow" viewBox="0 0 120 80">
        <path d="M20,40 Q50,35 110,40" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round"/>
        <path d="M80,15 L112,41 L80,65" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <div class="note-text-block">
        <span class="service-title-handwritten">IAM</span>
        <span class="map-handwritten-text">Guardián de la<br>seguridad perimetral</span>
      </div>
    </div>
  </div>

  <div class="map-row row-buckets">
    <div class="node node-medium">
      <div class="node-content"><img src="/bucket-logo.png" alt="Buckets" class="node-logo" /></div>
      <div class="node-popup"><p><strong>Pandora:</strong> Contenedores de datos específicos.</p></div>
    </div>
    <div class="map-side-note note-right">
      <svg class="marker-arrow" viewBox="0 0 120 80">
        <path d="M20,40 Q50,35 110,40" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round"/>
        <path d="M80,15 L112,41 L80,65" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <div class="note-text-block">
        <span class="service-title-handwritten">BUCKETS</span>
        <span class="map-handwritten-text">Depósitos de datos<br>críticos estructurados</span>
      </div>
    </div>
  </div>

  <div class="map-split-row row-split-middle">
    <div class="split-zone left-zone">
      <div class="map-side-note note-left">
        <div class="note-text-block text-right">
          <span class="service-title-handwritten">LAMBDA</span>
          <span class="map-handwritten-text">Código reactivo<br>bajo demanda</span>
        </div>
        <svg class="marker-arrow reverse-arrow" viewBox="0 0 120 80">
          <path d="M20,40 Q50,35 110,40" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round"/>
          <path d="M80,15 L112,41 L80,65" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>
      <div class="node node-small static-node">
        <div class="node-content"><img src="/lambda-logo.png" alt="Lambda" class="node-logo" /></div>
        <div class="node-popup popup-right"><p><strong>Zeus:</strong> Cómputo serverless inmediato reactivo.</p></div>
      </div>
    </div>
    <div class="split-zone right-zone">
      <div class="node node-small static-node">
        <div class="node-content"><img src="/crawler-logo.png" alt="Crawlers" class="node-logo" /></div>
        <div class="node-popup popup-left"><p><strong>Argos:</strong> Mapea y estructura metadatos ocultos.</p></div>
      </div>
      <div class="map-side-note note-right">
        <svg class="marker-arrow" viewBox="0 0 120 80">
          <path d="M20,40 Q50,35 110,40" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round"/>
          <path d="M80,15 L112,41 L80,65" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <div class="note-text-block">
          <span class="service-title-handwritten">CRAWLERS</span>
          <span class="map-handwritten-text">Exploradores de<br>esquemas activos</span>
        </div>
      </div>
    </div>
  </div>

  <div class="map-row row-glue">
    <div class="node node-large">
      <div class="node-content"><img src="/glue-logo.png" alt="Glue" class="node-logo" /></div>
      <div class="node-popup"><p><strong>Hermes:</strong> Orquestador ETL encargado de transformar datos.</p></div>
    </div>
    <div class="map-side-note note-right">
      <svg class="marker-arrow" viewBox="0 0 120 80">
        <path d="M20,40 Q50,35 110,40" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round"/>
        <path d="M80,15 L112,41 L80,65" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <div class="note-text-block">
        <span class="service-title-handwritten">GLUE</span>
        <span class="map-handwritten-text">El mensajero y ETL<br>de los datos</span>
      </div>
    </div>
  </div>

  <div class="map-row row-athena">
    <div class="node node-medium">
      <div class="node-content"><img src="/athena-logo.png" alt="Athena" class="node-logo" /></div>
      <div class="node-popup"><p><strong>Atenea:</strong> Motor de análisis interactivo SQL directo.</p></div>
    </div>
    <div class="map-side-note note-right">
      <svg class="marker-arrow" viewBox="0 0 120 80">
        <path d="M20,40 Q50,35 110,40" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round"/>
        <path d="M80,15 L112,41 L80,65" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <div class="note-text-block">
        <span class="service-title-handwritten">ATHENA</span>
        <span class="map-handwritten-text">Consultas ágiles<br>con sabiduría</span>
      </div>
    </div>
  </div>

  <div class="map-row row-final">
    <div class="node node-small">
      <div class="node-content"><img src="/demo-logo.png" alt="Demo" class="node-logo" /></div>
      <div class="node-popup"><p><strong>Efesto:</strong> Confluencia final de la interfaz funcional.</p></div>
    </div>
    <div class="map-side-note note-right">
      <svg class="marker-arrow" viewBox="0 0 120 80">
        <path d="M20,40 Q50,35 110,40" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round"/>
        <path d="M80,15 L112,41 L80,65" stroke="#d3d3d3" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <div class="note-text-block">
        <span class="service-title-handwritten">DEMO</span>
        <span class="map-handwritten-text">Ecosistema forjado<br>en armonía</span>
      </div>
    </div>
  </div>

</section>

<style>
/* --- CONTENEDOR FLUIDO ANTI-COLISIÓN --- */
.architecture-wrapper {
  position: relative;
  width: 100%;
  min-height: 1550px;
  background: #0d0d0d;
  overflow: hidden;
  box-sizing: border-box;
}

/* NAVBAR */
.map-navigation-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 40px;
  background: #000000;
  border-bottom: 2px solid #222222;
}
.logo-brand { font-size: 18px; font-weight: 900; color: #ffffff; text-decoration: none; }
.nav-logo-link { text-decoration: none; }
.nav-menu-btn { background: #ffffff; color: #000000; border: none; padding: 8px 16px; font-weight: 800; cursor: pointer; }

/* PANEL EXPLICATIVO */
.info-intro-card { position: absolute; top: 40px; left: 40px; width: 280px; z-index: 10; }
.card-shadow { position: absolute; width: 100%; height: 100%; background: #d3d3d3; top: 10px; left: 10px; z-index: 1; }
.card-content { position: relative; background: #ffffff; border: 2px solid #000000; padding: 20px; z-index: 2; color: #000000; }
.info-intro-card h4 { margin: 0 0 8px 0; font-size: 15px; }
.info-intro-card p { font-size: 13px; line-height: 1.4; margin: 0 0 10px 0; }
.analogy-box { border-top: 1px solid #ddd; padding-top: 8px; }
.analogy-box .handwritten-title { font-family: 'Caveat', cursive; font-size: 24px; font-weight: 700; }

/* --- MAQUETACIÓN POR FILAS (BLINDAJE TOTAL) --- */
.map-row {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 5;
}

/* Posiciones exactas en el eje Y */
.row-s3      { top: 50px; }
.row-iam     { top: 240px; left: 69%; transform: translateX(-50%); } /* Sube a su rama lateral derecha */
.row-buckets { top: 420px; }
.row-glue    { top: 860px; }
.row-athena  { top: 1100px; }
.row-final   { top: 1310px; }

/* Fila especial dividida para Lambda y Crawlers */
.map-split-row {
  position: absolute;
  top: 630px;
  width: 100%;
  display: flex;
  justify-content: center;
  z-index: 5;
}
.split-zone { width: 400px; display: flex; align-items: center; }
.left-zone { justify-content: flex-end; padding-right: 140px; }
.right-zone { justify-content: flex-start; padding-left: 140px; }

/* --- CÍRCULOS (JERARQUÍA COMPLETA) --- */
.node { cursor: pointer; position: relative; flex-shrink: 0; }
.static-node { position: relative; left: 0 !important; transform: none !important; }

.node-content {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #ffffff;
  border: 2px solid #000000;
  box-sizing: border-box;
  padding: 15px;
  transition: all 0.3s ease;
}
.node:hover .node-content {
  box-shadow: 0 0 35px 12px rgba(255, 255, 255, 0.85);
  transform: scale(1.05);
}

/* Protege el tamaño si la imagen falla */
.node-logo { 
  width: 65px; 
  height: 65px; 
  object-fit: contain; 
  display: block;
}

/* Asignación estricta de diámetros */
.node-large { width: 170px; height: 170px; }
.node-medium { width: 150px; height: 150px; }
.node-small { width: 135px; height: 135px; }

/* --- SISTEMA DE FLECHAS Y TEXTOS ALINEADOS --- */
.map-side-note {
  display: flex;
  align-items: center;
  pointer-events: none;
  gap: 10px;
}
.note-right { padding-left: 15px; } /* Espaciado de seguridad desde el círculo */
.note-left  { padding-right: 15px; }

.note-text-block { display: flex; flex-direction: column; width: 210px; }
.text-right { text-align: right; }

.service-title-handwritten {
  font-family: 'Caveat', cursive;
  font-size: 24px;
  font-weight: 800;
  color: #ffffff;
  line-height: 1;
  margin-bottom: 4px;
}
.map-handwritten-text {
  font-family: 'Caveat', cursive;
  font-size: 19px; 
  font-weight: 500;
  color: #c5c5c5;
  line-height: 1.1;
}

/* Flechas ultra-gruesas corregidas */
.marker-arrow { width: 55px; height: 40px; flex-shrink: 0; overflow: visible; }
.reverse-arrow { transform: scaleX(-1); }

/* --- LÍNEAS DE FONDO SVG --- */
.architecture-lines { position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none; }
.main-map-line { fill: none; stroke: url(#main-vertical-flow); stroke-width: 6; }
.animated-main-flow { stroke-dasharray: 50 30; animation: centralStream 4s linear infinite; }
@keyframes centralStream { to { stroke-dashoffset: -80; } }

.side-map-line { fill: none; stroke: url(#side-diagonal-flow); stroke-width: 4; }
.animated-side-flow { stroke-dasharray: 1000; stroke-dashoffset: 1000; animation: lateralStream 6s linear infinite; }
@keyframes lateralStream { to { stroke-dashoffset: 0; } }

/* POPUPS FLOTANTES */
.node-popup {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 230px;
  background: #ffffff;
  border: 2px solid #000000;
  padding: 14px;
  opacity: 0;
  pointer-events: none;
  transition: all 0.25s ease;
  font-size: 13px;
  color: #000000;
  box-shadow: 8px 8px 0px rgba(255, 255, 255, 0.1);
  z-index: 30;
}
.node:hover .node-popup { opacity: 1; }
.node-popup.popup-left { right: 115%; }
.node-popup.popup-right { left: 115%; }

/* RESPONSIVE CORREGIDO */
@media (max-width: 1100px) {
  .architecture-wrapper { min-height: 2600px; }
  .map-row { position: relative; left: 50% !important; transform: translateX(-50%) !important; top: auto !important; margin-bottom: 40px; }
  .map-split-row { position: relative; top: auto; flex-direction: column; align-items: center; }
  .split-zone { width: 100%; justify-content: center !important; padding: 0 !important; margin-bottom: 40px; }
  .map-side-note { display: none; }
}
</style>
